doctype html
html(lang='en')
  head
    title= title
  body
    h1 Пример работы шаблонизатора
    form(method='get', action='/logout')
     input.btn.btn-primary(type='submit', value='Разлогиниться')
    #container
      p Список логинов:
      ol
        each item in items
          li= item.login
     
